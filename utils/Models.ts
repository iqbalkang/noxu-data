export type TableRow = {
  id: number;
  name: string;
  industry: string;
  city: string;
  state: string;
  segment: string;
  ownerID: number;
};

export type TableHeading = {
  image?: string;
  title?: string;
};
